# API base URL used by the frontend (change for different environments)
# Default local development value:
REACT_APP_API_URL=http://localhost:5000/api

# For production on Vercel (or other hosts) you can point to the deployed backend on Render:
# REACT_APP_API_URL=https://task-manager-backend-2550.onrender.com/api
